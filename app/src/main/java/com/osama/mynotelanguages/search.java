package com.osama.mynotelanguages;import androidx.appcompat.app.AlertDialog;import androidx.appcompat.app.AppCompatActivity;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import android.content.Intent;import android.database.Cursor;import android.os.Bundle;import android.os.Handler;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.TextView;import com.google.android.material.floatingactionbutton.FloatingActionButton;import java.util.ArrayList;public class search extends AppCompatActivity {//    RecyclerView recyclerView;//    Customadapter customAdapter;//    ArrayList<String> word , meaning;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_search);        EditText search_inp = findViewById(R.id.search_inp);        EditText se = findViewById(R.id.search_inp2);//        word = new ArrayList<>();//        meaning = new ArrayList<>();//        if (!getIntent().hasExtra("word")) {            AlertDialog.Builder builder = new AlertDialog.Builder(search.this);            builder.setTitle("Error")                    .setIcon(R.drawable.ic_baseline_error_24)                    .setMessage("NO DATA");            AlertDialog dialog = builder.create();            dialog.show();        }        String word = getIntent().getStringExtra("word");        String meaning = getIntent().getStringExtra("meaning");        String intent = getIntent().getStringExtra("intent");        search_inp.setText(word);        se.setText(meaning);            Button delete_fr = findViewById(R.id.Delete_fr);            delete_fr.setOnClickListener(new View.OnClickListener() {                @Override                public void onClick(View v) {                    try {                        SqliteFr sqliteFr = new SqliteFr(search.this);                        sqliteFr.delete_fr(word);                        Sqlite sqlite = new Sqlite(search.this);                        sqlite.delete(word);                        Handler handler = new Handler();                        handler.postDelayed(new Runnable() {                            @Override                            public void run() {                                Intent intent = new Intent(search.this , MainActivity.class);                                startActivity(intent);                            }                        },1000);                    }                    catch (Exception e){                        Sqlite sqlite = new Sqlite(search.this);                        sqlite.delete(word);                    }                }            });        }            }