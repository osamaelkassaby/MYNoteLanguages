package com.osama.mynotelanguages;import androidx.appcompat.app.AlertDialog;import androidx.appcompat.app.AppCompatActivity;import androidx.recyclerview.widget.LinearLayoutManager;import androidx.recyclerview.widget.RecyclerView;import android.content.Intent;import android.database.Cursor;import android.os.Bundle;import android.view.View;import android.view.Window;import android.view.WindowManager;import com.google.android.material.floatingactionbutton.FloatingActionButton;import java.util.ArrayList;public class show_fr_words extends AppCompatActivity {    RecyclerView recyclerView_fr;    Customadapter customAdapter_fr;    ArrayList<String> word_fr , meaning_fr;    String token_ , email_;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        requestWindowFeature(Window.FEATURE_NO_TITLE);        getWindow().setFlags(WindowManager.LayoutParams.FLAG_FULLSCREEN,                WindowManager.LayoutParams.FLAG_FULLSCREEN);        setContentView(R.layout.activity_show_fr_words);        word_fr = new ArrayList<>();        meaning_fr = new ArrayList<>();        FloatingActionButton search = findViewById(R.id.search_fr);        search.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                Intent intent = new Intent(show_fr_words.this, show_backup.class);                intent.putExtra("intent","fr");                startActivity(intent);            }        });        User user = new User(show_fr_words.this);        Cursor cursor_usr = user.redData();        if(cursor_usr.getCount() == 0){        }else {            while (cursor_usr.moveToNext()) {                //    token.add(cursor_usr.getString(4));                //    email.add(cursor_usr.getString(2));                token_ = cursor_usr.getString(4);                email_ = cursor_usr.getString(2);            }        }        SqliteFr sqliteFr = new SqliteFr(show_fr_words.this);        Cursor cursor = sqliteFr.redData();        if (cursor.getCount() == 0) {            AlertDialog.Builder builder = new AlertDialog.Builder(show_fr_words.this);            builder.setIcon(R.drawable.ic_baseline_error_24)                    .setMessage("No Date");            AlertDialog dialog = builder.create();            dialog.show();        } else {            while (cursor.moveToNext()) {                word_fr.add(cursor.getString(0));                meaning_fr.add(cursor.getString(1));            }        }        recyclerView_fr = findViewById(R.id.recyclerView_fr);        customAdapter_fr = new Customadapter(show_fr_words.this, word_fr , meaning_fr ,"short", token_ ,email_);        recyclerView_fr.setAdapter(customAdapter_fr);        recyclerView_fr.setLayoutManager(new LinearLayoutManager(show_fr_words.this));    }    }